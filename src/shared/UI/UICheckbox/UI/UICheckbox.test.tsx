import { render, screen } from "@testing-library/react";
import { UICheckbox } from "./UICheckbox";

// describe("UICheckbox", () => {
//   test("Test render ", () => {
//     render(<UICheckbox active onChange={() => ({})} />);
//     expect(screen.getByTestId("UICheckbox")).toBeInTheDocument();
//   });
// });
